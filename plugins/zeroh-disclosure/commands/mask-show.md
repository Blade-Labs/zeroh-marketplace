---
description: Show what the model saw this session - tokens, types, sources and counts, never values
allowed-tools: Bash(node "${CLAUDE_PLUGIN_ROOT}/commands/scripts/show.js"), PowerShell(node "${CLAUDE_PLUGIN_ROOT}/commands/scripts/show.js")
---

The read-only command output is already loaded below. Present it verbatim in a fenced text block. Do not use tools or add commentary.

!`node "${CLAUDE_PLUGIN_ROOT}/commands/scripts/show.js"`
