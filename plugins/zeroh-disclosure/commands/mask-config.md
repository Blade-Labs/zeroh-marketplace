---
description: Show ZeroH Disclosure configuration, policy and protection engine
allowed-tools: Bash(node "${CLAUDE_PLUGIN_ROOT}/commands/scripts/config.js"), PowerShell(node "${CLAUDE_PLUGIN_ROOT}/commands/scripts/config.js")
---

The read-only command output is already loaded below. Present it verbatim in a fenced text block. Do not use tools or add commentary.

!`node "${CLAUDE_PLUGIN_ROOT}/commands/scripts/config.js"`
