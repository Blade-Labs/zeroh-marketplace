---
description: Print the signed ZeroH Disclosure receipt for this session
allowed-tools: Bash(node "${CLAUDE_PLUGIN_ROOT}/commands/scripts/receipt.js"), PowerShell(node "${CLAUDE_PLUGIN_ROOT}/commands/scripts/receipt.js")
---

The read-only command output is already loaded below. Present it verbatim in a fenced text block. Do not use tools or add commentary.

!`node "${CLAUDE_PLUGIN_ROOT}/commands/scripts/receipt.js"`
