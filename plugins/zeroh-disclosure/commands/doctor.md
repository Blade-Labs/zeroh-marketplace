---
description: Check ZeroH Disclosure (proxy, settings entry, login item, vaults); --fix repairs, --report prints a paste-safe summary
argument-hint: '[--fix|--report]'
allowed-tools: Bash(node "${CLAUDE_PLUGIN_ROOT}/bin/zeroh-disclosure.mjs" doctor), PowerShell(node "${CLAUDE_PLUGIN_ROOT}/bin/zeroh-disclosure.mjs" doctor), Bash(node "${CLAUDE_PLUGIN_ROOT}/bin/zeroh-disclosure.mjs" doctor *), PowerShell(node "${CLAUDE_PLUGIN_ROOT}/bin/zeroh-disclosure.mjs" doctor *)
disable-model-invocation: true
---

The command output is already loaded below. Present it verbatim in a fenced text block. Do not use tools or add commentary.

!`node "${CLAUDE_PLUGIN_ROOT}/bin/zeroh-disclosure.mjs" doctor $ARGUMENTS`
